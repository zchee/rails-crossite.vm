# rbenv
export RBENV_ROOT="${HOME}/.rbenv"
if [ -d ${HOME}/.rbenv ]; then
  export PATH="${RBENV_ROOT}/bin:$PATH"
  eval "$(rbenv init -)"
fi

# ruby_build patch
alias 'rbenv install'='curl -fsSL https://gist.github.com/mislav/a18b9d7f0dc5b9efc162.txt | rbenv install --patch'

function chpwd() { ls -AF }
